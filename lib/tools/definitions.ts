import { webSearch } from "./web-search";
import { saveToNotion } from "./save-to-notion";
import { sendDigest } from "./send-digest";

export const tools = {
  webSearch,
  saveToNotion,
  sendDigest,
};